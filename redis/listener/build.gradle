plugins {
    id "application"
}

archivesBaseName = 'redis.' + project.name

application {
    mainClassName = "example.redis.ListenerApp"
}

dependencies {
    compile project(":redis:common")
}