plugins {
    id "application"
}

archivesBaseName = 'rabbitMq.' + project.name

application {
    mainClassName = "example.rabbitMq.ListenerApp"
}

dependencies {
    compile project(":rabbitMq:common")
}